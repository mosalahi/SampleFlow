# SampleFlow — نظام متابعة العينات اليومية

## الفكرة
نظام ويب لتجميع بيانات العينات اليومية من عدة مراكز صحية في مكان واحد.
حالياً كل مركز يعبّي ملف Excel يومياً، والمشرف يجمّعها يدوياً — طريقة متعبة.
الهدف: كل مركز يُدخل بياناته مباشرة، والتجميع يصير **تلقائياً** في لوحة المشرف.

## القرارات المتفق عليها
- **Backend:** ASP.NET Core (MVC + واجهة، أو Web API + واجهة)
- **قاعدة البيانات:** PostgreSQL
- **المصادقة:** ASP.NET Core **Identity** (Roles + تشفير كلمات المرور جاهز)
- **الاستضافة:** VPS خاص (Contabo) عبر IIS/Kestrel + HTTPS
- **الدخول:** حساب موحّد لكل مركز — قابل للتوسّع لمستخدمين أفراد لاحقاً
- **مستوى البيانات:** أرقام تجميعية يومية فقط (بدون بيانات مرضى/أسماء)
- **الواجهة:** عربية RTL، نظيفة، متجاوبة مع الجوال
- **التاريخ:** ميلادي بصيغة yyyy/MM/dd
- **المرونة:** المراكز وأنواع التحاليل **تُدار من لوحة التحكم** (بدون تعديل الكود أو قاعدة البيانات)

## الصلاحيات (Roles)
- **Center** — يدخل بيانات مركزه فقط، ويشوف إدخالاته.
- **Supervisor** — يشوف كل المراكز، التقارير، والتصدير.
- **Admin** — كل ما سبق + إدارة المراكز وأنواع التحاليل والمستخدمين.

---

## قاعدة البيانات

### Identity (يولّدها الـ framework)
`AspNetUsers`, `AspNetRoles`, `AspNetUserRoles` … إلخ.
نوسّع `ApplicationUser` بحقل:
- `CenterId` (int?, FK → Centers) — يربط مستخدم المركز بمركزه. للمشرف/الأدمن يكون null.

### Centers — المراكز (مرن)
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| Name | string | اسم المركز |
| IsActive | bool | تعطيل بدل الحذف |
| CreatedAt | datetime | |

بيانات أولية (seed): 16 مركز — الأشرفية، الصالحية، القادسية، المطار، السليمانية، الزهرة، الروضة، لولوة النعيم، غرب عنيزة، الروابي، الروغاني، جنوب غرب عنيزة، وسط عنيزة، الصناعية، الملك خالد، جنوب الملك خالد.
> تُدار بالكامل من لوحة الأدمن (إضافة/تعطيل).

### SampleTypes — أنواع التحاليل (مرن)
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| Code | string | CBC, HBA1C … (فريد) |
| Name | string | الاسم المعروض |
| DisplayOrder | int | ترتيب الظهور في الشاشة |
| IsActive | bool | تعطيل بدل الحذف |

بيانات أولية (seed): CBC, Chemistry, HBA1C, Hormone, Stool, FIT, Urine, ABO, Serology.
> إضافة نوع جديد = صف جديد هنا، وشاشة الإدخال **تبنيه تلقائياً** دون تعديل كود.

### DailyEntries — إدخال اليوم (رأس السجل)
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| CenterId | int FK | → Centers |
| EntryDate | date | |
| Visitors | int | عدد المراجعين |
| Trips | int | عدد الرحلات |
| CreatedByUserId | string FK | → AspNetUsers |
| CreatedAt | datetime | |
| UpdatedByUserId | string FK? | آخر من عدّل |
| UpdatedAt | datetime? | |

**قيد مهم:** Unique index على (CenterId + EntryDate) — إدخال واحد لكل مركز/يوم.

### DailyEntryDetails — تفاصيل العينات (مرن)
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| DailyEntryId | int FK | → DailyEntries (Cascade) |
| SampleTypeId | int FK | → SampleTypes |
| Count | int | عدد العينات لهذا النوع |

> صف لكل نوع تحليل داخل إدخال اليوم. هذا ما يجعل التحاليل مرنة.
> Unique index على (DailyEntryId + SampleTypeId).

### AuditLogs — سجل التتبع
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | long PK | |
| UserId | string | من قام بالإجراء |
| UserName | string | (لقطة الاسم وقت الحدث) |
| Action | string | Create / Update / Delete / Login |
| EntityName | string | اسم الجدول |
| EntityId | string | معرّف السجل المتأثر |
| OldValues | text (JSON) | القيم قبل التعديل |
| NewValues | text (JSON) | القيم بعد التعديل |
| Timestamp | datetime | |
| IpAddress | string | |

> يُملأ تلقائياً عبر اعتراض SaveChanges في الـ DbContext (تتبّع مركزي لكل التغييرات).

---

## الشاشات

### 1. تسجيل الدخول (Identity)
- Center ← شاشة الإدخال. Supervisor/Admin ← لوحة المشرف.

### 2. إدخال المركز اليومي
- المركز مقفل حسب حساب المستخدم.
- الحقول: التاريخ · المراجعين · الرحلات.
- **حقول التحاليل تُبنى ديناميكياً** من SampleTypes النشطة (مرتبة حسب DisplayOrder).
- زر حفظ ← يمنع التكرار لنفس اليوم (تنبيه أو فتح للتعديل حسب الصلاحية).
- يعرض إجمالي عينات اليوم.

### 3. لوحة المشرف
- **KPIs:** إجمالي العينات · إجمالي المراجعين · المراكز التي أدخلت (X/إجمالي النشطة) · الأعلى نشاطاً.
- **فلترة:** من/إلى تاريخ · المركز · نوع التحليل.
- **جدول تجميعي:** صف لكل مركز + حالة (أُدخلت/بانتظار) + أعمدة التحاليل (ديناميكية) + صف إجمالي.
- **رسوم:** العينات حسب المركز · توزيع أنواع التحاليل.
- **تصدير:** Excel و PDF.

### 4. لوحة الإدارة (Admin)
- إدارة المراكز (إضافة/تعطيل).
- إدارة أنواع التحاليل (إضافة/تعطيل/ترتيب).
- إدارة المستخدمين والصلاحيات.
- عرض AuditLogs مع فلترة.

---

## الأمان
- ASP.NET Core Identity (Hash + Salt جاهز، Lockout).
- تقييد كل مركز بإدخاله فقط (Authorization بالـ CenterId).
- HTTPS إلزامي.
- AuditLog لكل إضافة/تعديل/حذف/دخول.
- نسخ احتياطي دوري لقاعدة البيانات.

## نقاط مفتوحة (تُحسم مع العميل)
- معنى "عدد الرحلات" بالضبط.
- من يعدّل إدخال خاطئ: المركز (بنفس اليوم فقط؟) أم المشرف؟
- تقارير أسبوعية/شهرية بأي شكل؟
- تنبيه للمراكز المتأخرة عن الإدخال؟

## المرجع البصري
تصور HTML مبدئي (بيانات وهمية) يوضّح الشكل النهائي:
https://mosalahi.github.io/SampleFlow/

## ترتيب التنفيذ المقترح (مراحل)
1. المشروع + Identity + DbContext + الـ Models.
2. Migration أولية + Seed (مراكز، أنواع تحاليل، أدوار، مستخدم أدمن).
3. اعتراض AuditLog في DbContext.
4. المصادقة والتوجيه حسب الدور.
5. شاشة الإدخال الديناميكية.
6. لوحة المشرف (تجميع + فلترة).
7. التصدير (Excel/PDF).
8. لوحة الإدارة.
