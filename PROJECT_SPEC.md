# SampleFlow — نظام متابعة العينات اليومية

## الفكرة
نظام ويب لتجميع بيانات العينات اليومية من عدة مراكز صحية في مكان واحد.
حالياً كل مركز يعبّي ملف Excel يومياً، والمشرف يجمّعها يدوياً — طريقة متعبة.
الهدف: كل مركز يُدخل بياناته مباشرة، والتجميع يصير **تلقائياً** في لوحة المشرف.

## القرارات المتفق عليها
- **Backend:** ASP.NET Core (MVC أو Web API + واجهة)
- **قاعدة البيانات:** PostgreSQL
- **الاستضافة:** VPS خاص (Contabo) عبر IIS/Kestrel
- **الدخول:** حساب موحّد لكل مركز (مو لكل موظف) — قابل للتوسّع لاحقاً لمستخدمين أفراد
- **مستوى البيانات:** أرقام تجميعية يومية فقط (بدون بيانات مرضى/أسماء) — الحساسية منخفضة
- **الواجهة:** عربية RTL، نظيفة، متجاوبة مع الجوال
- **التاريخ:** ميلادي بصيغة yyyy/MM/dd

## قاعدة البيانات (3 جداول)

### Centers — المراكز
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| Name | string | اسم المركز |
| Username | string | لتسجيل الدخول |
| PasswordHash | string | |
| IsActive | bool | |

بيانات أولية: 16 مركز (الأشرفية، الصالحية، القادسية، المطار، السليمانية، الزهرة، الروضة، لولوة النعيم، غرب عنيزة، الروابي، الروغاني، جنوب غرب عنيزة، وسط عنيزة، الصناعية، الملك خالد، جنوب الملك خالد).

### DailyEntries — الإدخالات اليومية
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| CenterId | int FK | → Centers |
| EntryDate | date | |
| Visitors | int | عدد المراجعين |
| Trips | int | عدد الرحلات |
| CBC | int | |
| Chemistry | int | |
| HBA1C | int | |
| Hormone | int | |
| Stool | int | |
| FIT | int | |
| Urine | int | |
| ABO | int | |
| Serology | int | |
| CreatedAt | datetime | |

**قيد مهم:** إدخال واحد فقط لكل (CenterId + EntryDate) — Unique index.

### Users — مستخدمو الإشراف
| العمود | النوع | ملاحظة |
|---|---|---|
| Id | int PK | |
| Username | string | |
| PasswordHash | string | |
| Role | string | Supervisor / Admin |

## الشاشات (3)

### 1. تسجيل الدخول
- المركز يدخل بحسابه → يروح لشاشة الإدخال.
- المشرف يدخل بحساب مستقل → يروح للوحة المشرف.

### 2. إدخال المركز اليومي
- الحقول: المركز (مقفل حسب الحساب) · التاريخ · المراجعين · الرحلات · 9 تحاليل (CBC, Chemistry, HBA1C, Hormone, Stool, FIT, Urine, ABO, Serology).
- زر حفظ → يمنع التكرار لنفس اليوم (يعرض تنبيه لو موجود، أو يفتح للتعديل).
- يعرض إجمالي عينات اليوم.

### 3. لوحة المشرف
- **KPIs:** إجمالي العينات اليوم · إجمالي المراجعين · عدد المراكز التي أدخلت (X/16) · الأعلى نشاطاً.
- **فلترة:** من/إلى تاريخ · المركز · نوع التحليل.
- **جدول تجميعي:** صف لكل مركز + حالة (أُدخلت/بانتظار) + الأعمدة + صف الإجمالي.
- **رسوم بسيطة:** العينات حسب المركز · توزيع أنواع التحاليل.
- **تصدير:** Excel و PDF.

## الأمان
- تشفير كلمات المرور (Hash + Salt).
- تقييد كل مركز بإدخاله فقط.
- نسخ احتياطي دوري لقاعدة البيانات.
- HTTPS على الاستضافة.

## نقاط مفتوحة (تُحسم مع العميل)
- معنى "عدد الرحلات" بالضبط.
- هل الأعمدة الـ9 ثابتة أم قد تزيد أنواع تحاليل؟
- من يعدّل إدخال خاطئ: المركز أم المشرف فقط؟
- تقارير أسبوعية/شهرية بأي شكل؟
- تنبيه للمراكز المتأخرة عن الإدخال؟

## المرجع البصري
تصور HTML مبدئي (بيانات وهمية) يوضّح الشكل النهائي المطلوب:
https://mosalahi.github.io/SampleFlow/
